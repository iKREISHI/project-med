import { FC } from "react";
import { Box, Typography } from "@mui/material";

export const PatientCheckup: FC = () => {
  return (
    <Box sx={{p: 1}}>
      <Typography variant="h1" gutterBottom>
      PatientCheckup
      </Typography>
    </Box>
  );
};